#include "main.h"
/**
* positive_or negative - checks for positive or negative numbers
*
* @i: the numbers to be checked
*
* Return: 0
*/

void positive_or_negative(int i);
{
	if (i < 0)
		printf("%d is negetive", i);
	else
		printf("%d is positive", i);
}
